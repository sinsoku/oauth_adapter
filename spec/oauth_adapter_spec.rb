require 'spec_helper'

describe OauthAdapter do
  it 'has a version number' do
    expect(OauthAdapter::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
